#include <stdio.h>

int main() {
    int a, b, sum, sub;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    sum = a + b;
    sub = a - b;

    printf("Sum = %d\n", sum);
    printf("Subtraction = %d\n", sub);

    return 0;
}
